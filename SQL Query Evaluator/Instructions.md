You are a python code generation agent to generate an UI solution where a SME can compare 2 sql scripts

Objective: Compare 2 sql scripts one generated from a self service tool and another the golden base line query created by SME.

Intention: The intention of the solution is to compare both the queries and by refining the tool prompts in an effective manner by following:
    a) Use JSON for Function Calls
    b) Add Internal Self-Check 
    c) Call Out Reasoning Type
    d) Fallbacks for Errors or Uncertainty
    e) Explicit Reasoning 
    f) Separation of Tools
    g) Conversation Loop with Memory Maintain a conversation_history and incrementally update the prompt, keeping the context fresh. This reinforces the idea that reasoning unfolded over multiple rounds
    h) Instructional Framing

Instructions:
1- Create a UI with below requirements:
    a) Heading: SME Query Validator
    b) a text box where business query can be pasted by SME
    b) Below the business query text box we need two text boxes one where SME can paste a SQL query generated by a self service tool and another text box where SME can paste his own query
    c) Compare button with click event
    d) a label to capture the pass fail result post Compare button click in a user firendly way

2- The pasted queries are based on tables from "SyntheticClaims.accdb" database. Please convert this database into SQLlite so that sql queries can be executed without any issue. For any database creation use the "Database" Folder

3- On click of Compare button 3 tools needs to be called: 
    a) Tool 1: Measure the Metric identification if its selected correctly. Does solution choose the right KPI definition- GCO/MIS or Features. Did the system map the question to the correct metric definition from metric catalog or standard GCO/MIS metrics?
    Measure using:
    “Correct metric field or logic selected” (yes/no)
    “Correct dimensional grain” (e.g., claim vs line vs event)
    The prompt for this tool should consider a resoning based approach to get the Metric Match Score = (# of correct metric mappings) / (total metrics considered in the SME query)
    For non scalar it should consider the columns mapping and comparision
    The Weightage of this tool should be considered as 40%

    b) Tool 2: Query Formulation and check the Correctness of SQL transformation from business query. Compare generated SQL to a “gold” reference i.e the SME query 
    Score based on by breaking the SQL into Semantic components:
    Same tables/joins?
    Same filters & time windows?
    Same group-by grain?
    Same aggregations?
    SQL Equivalence = weighted overlap of semantic components
    The prompt should be created with some mathematical approach (like Jaccard similarity) through which the SQL Equivalence can be defined and calculated. This prompt resoning should have a chain of thought approach, break down in sub tasks.
    The Weightage of this tool should be considered as 40%

    c) Tool 3: This tool should execute both the queries against the SQL lite database (created using  "SyntheticClaims.accdb"). Are the results correct and plausible
    For single numbers: use relative error
    For distributions: use top-k overlap, correlation, or KS-distance
    Result Agreement = 1 − min( |Generated − Gold| / (Gold), 1 )
    The prompt reasoning should define how generated and Gold Reference i.e. SME queries, are used mathematically. Please specify the algorithms and define the steps in the prompt which needs to be done before doing the final calculation. The prompt chain of thought should consider both structural and functional correctness.
    The Weightage of this tool should be considered as 20%

    The matching score of individual tools and final Weightage matching score should be calculated and shown as well on UI along with the respective SQL execution results.

    

